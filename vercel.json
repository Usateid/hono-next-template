{
  "buildCommand": "cd web && bun run build",
  "devCommand": "cd web && bun run dev",
  "installCommand": "cd web && bun install",
  "outputDirectory": "web/.next",
  "framework": null,
  "ignoreCommand": "if git diff --quiet HEAD^ HEAD -- web/ ':!*.md' ':!*.txt' ':!LICENSE'; then echo 'No frontend changes'; exit 0; else echo 'Frontend changes detected'; exit 1; fi"
}

